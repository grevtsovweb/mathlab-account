include ../mixins/svg.pug

mixin feature(svg, text, className)
    .feature(class= className)
        .feature__icon
            .feature__icon-inner
              +svg(50, 50, svg)
        p.feature__text= text